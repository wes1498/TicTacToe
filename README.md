**Distributed Tic-Tac-Toe**
=============

To compile, cd to this directory and run:
```
make
```

To run the server:
```
./server [port number]
```

To run clients:
```
./client [server host] [port number]
```
